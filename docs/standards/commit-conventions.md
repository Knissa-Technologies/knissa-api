# Commit Convention

Pattern

type(scope): description

Examples

feat(wallet): add transfer validation

fix(auth): validate refresh token

docs(adr): add wallet architecture

refactor(transaction): simplify ledger service

test(wallet): add transfer tests